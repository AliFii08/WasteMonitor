// import { InjectionToken, NgModule } from '@angular/core';
// import type { app } from 'firebase-admin';


// export const FIREBASE_ADMIN = new InjectionToken<app.App>('firebase0-admin')

// @NgModule({
//   providers: [
//     {provide: FIREBASE_ADMIN, useFactory: () => FIREBASE_ADMIN.apps[0] || FIREBASE_ADMIN.initializeApp() }
//   ]
// })
